tunnel: ${CLOUDFLARED_TUNNEL_ID}
credentials-file: /etc/cloudflared/creds.json

ingress:
  - hostname: motus.${BASE_DOMAIN}
    service: ${PUBLIC_ORIGIN_SERVICE}
    originRequest:
      httpHostHeader: ${PUBLIC_HOST_HEADER}

  - service: http_status:404
